抽象工厂模式:
实现思路 模拟了消费者和汽车生产工厂的模式

1.工厂接口只有一个createCar接口,目的消费者使用工厂接口时,工厂接口只管生产汽车,至于生产什么类型的车由它的实现类完成。
2.工厂父类BMWFactory的静态方法getBMWFactory根据传递的类型枚举区分,返回不同的工厂,
    由于是BMW生产工厂所有其内部有个抽象方法createBMW,其子类必须实现createBMW方法。
    BMWFactory实现了IFactory接口方法,方法内部由当前对方执行createCar方法,这么做可以屏蔽客户端使用的复杂性。

3.产品的实现是有产品接口和实现产品接口的茶品类组成,产品接口的使用 是为了在工厂中使用时返回方法屏蔽类型的区分。
